services:
  - type: web
    name: backend-market-intelligence
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    pythonVersion: 3.10